// Calculates score for removing a series of given length

const calculateScore = (seriesLength, maxColors) => {
  // TEMPORARY - need to check actual scoring table
  return seriesLength;
};

export default calculateScore;
